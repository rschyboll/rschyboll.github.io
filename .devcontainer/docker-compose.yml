version: '3.8'
services:
  app:
    container_name: rschyboll.github.io
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
      - rschyboll.github.io-DEV-fish:/home/node/.local/share/fish
    init: true
    command: sleep infinity
    user: node
#    ports:
#      - '5173:5173'
#      - '4173:4173'

volumes:
  rschyboll.github.io-DEV-fish:
